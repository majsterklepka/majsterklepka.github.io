<div id="news">        
      <h2>Ostatnie wpisy:</h2>
      {% for post in site.posts limit: site.posts_number %}
		<h4><a href="{{ site.baseurl }}{{ post.url }}" title="{{ post.title }}">{{ post.title }}</a><span>author: {{ post.author }} &nbsp; {{ post.date | date_to_string }}</span></h4>
		{% endfor %}
</div>